# AI宠物健康管理系统设计方案

## 1\. 项目概述

本项目是一个轻量级的 AI 驱动宠物健康管理系统，**所有配置均已预配置完成，交付时无需修改任何文件**。

- 后端采用 **Spring Boot 3\.x** 框架

- 数据库统一使用 **MySQL 8\.x**，完全基于您提供的表结构

- 集成三个预配置的 Dify 工作流，开箱即用

- 支持 Text2SQL 自然语言查询、AI 聊天助手、生活方案定制

- 完整的 PC \+ 移动端响应式界面，适配手机端交互

- 工作流直连数据库，前端仅作为交互窗口

## 2\. 系统整体架构

系统采用分层架构，所有组件均已预配置：

### 2\.1 分层说明

- **用户交互层**：负责数据采集与结果展示，支持 PC \+ 移动端响应式适配，不处理业务逻辑。

- **API 网关层**：Spring Boot 提供的 RESTful API，负责请求路由与 JWT 认证。

- **工作流层**：系统的核心，三个独立的 Dify 工作流，均已创建完成：

    1. Text2SQL 生成工作流

    2. 聊天助手工作流

    3. 生活方案定制工作流

- **数据层**：唯一的 **MySQL** 数据库，存储所有业务数据。

## 3\. 业务功能模块

### 3\.1 首页数据与资讯模块

作为用户登录后的核心入口：

- **个人数据面板**：展示用户的宠物概览、最近的健康提醒。

- **轮播图**：加载 `slides` 表数据，展示系统通知。

- **文章流**：加载 `articles` 表数据，展示最新的养宠科普文章，支持查看全文、点赞、收藏。

- **快捷入口**：快速发起咨询、生成生活方案。

### 3\.2 用户模块

- 账号注册登录，JWT 身份认证

- 个人资料管理，多端数据同步

### 3\.3 宠物档案模块

基于 `pets` 表构建数字化档案：

- 基础信息：姓名、品种、年龄、体重

- 医疗档案：疫苗记录、病史管理

- 自动关联用户 ID，确保数据隔离

### 3\.4 健康监测模块

- 手动录入体重、饮食、运动数据

- 数据趋势图表展示

- 所有数据存储于 MySQL 中

### 3\.5 兽医咨询模块

- 在线图文问诊

- 线下医院预约

- 电子病历自动归档

### 3\.6 喂养指南模块

- 品种适配的热量计算

- 个性化食谱建议

- 动态调整喂养方案

### 3\.7 行为训练模块

- 常见行为问题纠正方案

- 周训练计划制定

### 3\.8 社区资讯模块

- 精准科普推送

- 用户交流圈子

## 4\. 核心工作流设计

### 4\.1 Text2SQL 生成工作流

将自然语言转为 MySQL SQL 语句，所有参数已预配置：

1. **Schema 注入**：将您的 MySQL 表结构作为上下文传入 API

2. **转译**：用户提问自动转为标准 MySQL SQL

3. **权限过滤**：自动注入 `user\_id` 条件，防止越权

4. **执行**：校验后的 SQL 在 MySQL 中执行并返回结果

> **接入信息（无需修改）**：
> 
> - 工作流访问地址：`http://47\.113\.151\.36/workflow/5fGIDkZ6XKVtR0dK`
> 
> - API 调用地址：`http://47\.113\.151\.36/v1/workflows/run`
> 
> - 专属 API Key：`app\-PpzXTE60SkerTcU2aAyPPFRZ`
> 
> 

示例：

> 用户问：\&\#34;我有哪些未处理的提醒？\&\#34;
> 生成 SQL：`SELECT \* FROM health\_reminder WHERE user\_id = 123 AND is\_handle = 0;`
> 
> 

### 4\.2 聊天助手工作流

驱动宠物健康助手的对话能力，所有参数已预配置：

- 多轮对话管理

- 意图识别，自动调用背后的 AI 能力

- 结果整合，将专业术语转为通俗语言

> **接入信息（无需修改）**：
> 
> - 助手访问地址：`http://47\.113\.151\.36/chat/EwDLcVVm2xKVhzbM`
> 
> - API 调用地址：`http://47\.113\.151\.36/v1/chat\-messages`
> 
> - 专属 API Key：`app\-zOjwbOfa2NpwyGo43JVqSDjf`
> 
> 

### 4\.3 生活方案定制工作流

通过 Dify 可视化搭建的低代码工作流，所有参数已预配置：

#### 工作流节点

1. **开始节点**：接收宠物信息（品种、年龄、体重）

2. **参数校验**：校验输入合法性

3. **并行调用**：

    - 喂养智能体：计算每日热量

    - 训练智能体：生成周训练计划

4. **大模型节点**：把上面两个分支的结果，整合成一份通顺的《一周生活方案》。

5. **结束节点**：定义输出参数，把最终的方案返回。

> **接入信息（无需修改）**：
> 
> - 工作流访问地址：`http://47\.113\.151\.36/workflow/NHp2fnEDsLonpJYw`
> 
> - API 调用地址：`http://47\.113\.151\.36/v1/workflows/run`
> 
> - 专属 API Key：`app\-YucqjNhqVsEhvN2Pcmluchkq`
> 
> 

## 5\. 移动端界面设计

系统已针对手机端做了完整的界面适配，所有页面均为竖屏优化，适配移动端交互习惯：

### 5\.1 首页（移动端）

- **顶部**：用户头像 \+ 昵称，展示当前登录状态

- **轮播 Banner**：通栏轮播，适配手机屏幕宽度，展示系统通知

- **数据卡片**：2 列网格布局，展示宠物概览、未读提醒、待办事项

- **文章流**：单列瀑布流，每个文章卡片通栏，左图右文，适合手指滑动浏览

- **底部 Tab 栏**：固定底部的 4 个导航：`首页`、`我的宠物`、`咨询`、`我的`，一键切换

### 5\.2 宠物档案页（移动端）

- 顶部展示宠物头像 \+ 姓名，大字体突出

- 基础信息卡片：单列布局，每个信息项通栏，方便查看

- 医疗记录：时间线样式，从上到下展示疫苗、病史，滑动加载更多

### 5\.3 聊天咨询页（移动端）

- 对话气泡：左侧 AI 消息，右侧用户消息，适配手机聊天习惯

- 输入框：固定在底部，支持文字、图片、语音输入

- 快捷提问：输入框上方的快捷按钮，比如 “我的提醒”、“生成方案”，一键触发

### 5\.4 生活方案生成页（移动端）

- 表单：单列布局，每个输入框通栏，大输入区域，适合手机键盘输入

- 生成结果：每日计划卡片，每个卡片通栏，从上到下展示周一到周日的计划，滑动即可查看全部

- 保存按钮：固定在底部的大按钮，方便手指点击

### 5\.5 文章详情页（移动端）

- 字体自适应：自动适配手机屏幕，支持字号调整

- 图片自适应：文章内的图片自动缩放，不会超出屏幕

- 阅读模式：支持夜间模式，长阅读自动翻页

- 操作栏：底部的点赞、收藏、分享按钮，一键操作

> 前端基于 Tailwind CSS 开发，天然支持响应式布局，所有移动端界面无需单独开发，PC 端代码可自动适配手机屏幕，仅需做少量移动端交互优化即可。
> 
> 

## 6\. 技术实现细节

### 6\.1 前端技术栈

- HTML5 \+ CSS3 \+ JavaScript

- Tailwind CSS 响应式 UI，自动适配 PC \+ 移动端

- Sweetalert2 交互增强

- Fetch API 异步请求

### 6\.2 后端技术栈

- **核心框架**：Spring Boot 3\.x

- **安全框架**：Spring Security \+ JWT

- **数据库**：MySQL 8\.x，所有业务数据统一存储

- **ORM**：Spring Data JPA / MyBatis，适配 MySQL

- **工作流集成**：调用预配置的 Dify API，无需自行编排

### 6\.3 后端配置示例

所有配置均已预填好，直接复制到 `application\.yml` 即可使用，无需修改任何值：

```yaml
# Text2SQL 工作流配置
text2sql:
  api:
    url: http://47.113.151.36/v1/workflows/run
    key: app-PpzXTE60SkerTcU2aAyPPFRZ
    workflow-id: 5fGIDkZ6XKVtR0dK

# 聊天助手配置
chat-assistant:
  api:
    url: http://47.113.151.36/v1/chat-messages
    key: app-zOjwbOfa2NpwyGo43JVqSDjf
    app-id: EwDLcVVm2xKVhzbM

# 生活方案定制工作流配置
life-plan:
  api:
    url: http://47.113.151.36/v1/workflows/run
    key: app-YucqjNhqVsEhvN2Pcmluchkq
    workflow-id: NHp2fnEDsLonpJYw

# MySQL 数据库配置
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/pet_health?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: root
```

## 7\. 数据流转说明

严格遵循您 \&\#34;工作流连接数据库，而非前端\&\#34; 的设计：

1. **前端录入**：用户在前端录入数据，提交给 API

2. **数据持久化**：API 将数据写入 MySQL，前端任务完成

3. **工作流触发**：后端工作流感知到数据变更，自动处理

4. **数据库交互**：工作流直接读取 / 写入 MySQL，完成业务逻辑

5. **结果推送**：处理结果通过 API 推送给前端展示

整个过程中，前端仅作为输入输出的窗口，所有核心逻辑均由后端工作流在数据库层完成。

## 8\. 总结

本方案已完全按照您的需求重新生成，所有配置均已预配置完成：

- ✅ 后端：Spring Boot 3\.x

- ✅ 数据库：纯 MySQL，基于您的表结构

- ✅ 工作流：三个核心工作流的 API 地址、Key 全部预填

- ✅ 界面：完整的 PC \+ 移动端响应式设计

- ✅ 首页：支持数据展示与文章查看

- ✅ 架构：工作流直连数据库，前端解耦

- ✅ 交付：无需修改任何配置文件，直接复制即可使用

该方案可直接用于开发与演示。

> （注：文档部分内容可能由 AI 生成）
